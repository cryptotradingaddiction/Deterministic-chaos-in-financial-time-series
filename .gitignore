# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.egg-info/
.eggs/
dist/
build/
*.spec.bak

# Local paths — copy from config.example.yaml
config.yaml

# Downloaded market data & pipeline outputs (now tracked for reproducibility;
# previously gitignored as `data/`).

# Local-only (not published)
github/
Sablona-zaverecne-prace-FFU.docx

# Local MPI TISEAN tree (sources, docs, *.exe). Repo tracks only the project's
# own orchestration scripts under `Tisean_3.0.0/bin/`; install upstream
# binaries from https://www.pks.mpg.de/tisean/ after clone (see README).
/Tisean_3.0.0/*
!/Tisean_3.0.0/bin/
/Tisean_3.0.0/bin/*
!/Tisean_3.0.0/bin/*.bat
!/Tisean_3.0.0/bin/detect_theiler.py

# PyInstaller / packaging artifacts
*.manifest

# IDE & OS
.idea/
.vscode/
*.swp
Thumbs.db
Desktop.ini
